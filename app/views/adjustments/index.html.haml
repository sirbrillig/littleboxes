= simple_form_for(:adjustment, :defaults => { :input_html => { :class => 'input-xlarge' } }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :action, collection: ['Decrement', 'Increment', 'Set']
    = f.input :name, autofocus: true
    = f.input :delta

  .form-actions
    = f.submit "Save", :class => "btn-primary"
